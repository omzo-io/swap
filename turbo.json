{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**"],
      "dependsOn": ["^build"]
    },
    "dev": {
      "outputs": [".next/**"],
      "dependsOn": ["^build"]
    }
  }
}
